#include <stdio.h>
#include <string.h>

/* Print one line using puts */
int main(void)
{
	char str[54];

	strcpy(str, "\"Programming is like building a multilingual puzzle\n\"");
	puts(str);
	return (0);
}
